CLEAR SCREEN
SET SERVEROUTPUT ON

DECLARE
    REC REGIONS%ROWTYPE;
BEGIN
    FOR RID IN 1..4 LOOP
	    SELECT * INTO REC FROM REGIONS WHERE REGION_ID = RID;
        DBMS_OUTPUT.PUT_LINE( REC.REGION_ID || ' ' || REC.REGION_NAME);
    END LOOP;
END;
/
